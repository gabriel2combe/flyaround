{% extends 'base.html.twig' %}

{% block body %}
<h1>New Review</h1>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <input type="submit" value="New" />
    {{ form_end(form) }}
<ul>
    <li>
   <a href="{{ path('review_index') }}"> Review List</a>
   </li>
</ul>
{% endblock %}